import RoleForm from '../components/onboarding/components/role-form';

export default function DashboardTestsPage() {
  return (
    <div>
      <RoleForm onSuccess={() => {}} onBack={() => {}} />
    </div>
  );
}